import type { MapCrs } from '../../shared/map';
import type { IWindFlowRenderFrame, IWindFlowRenderer } from './types';
type WindFlowWebglBackend = 'webgl' | 'webgl2';
export declare class WebglWindFlowRenderer implements IWindFlowRenderer {
    readonly backend: WindFlowWebglBackend;
    private canvas;
    private gl;
    private fadeProgram;
    private segmentProgram;
    private clipProgram;
    private fullscreenBuffer;
    private segmentBuffer;
    private clipBuffer;
    private width;
    private height;
    private readonly contextLostHandler;
    private readonly contextRestoredHandler;
    constructor(backend: WindFlowWebglBackend);
    initialize(canvas: HTMLCanvasElement, _crs: MapCrs): void;
    render(frame: IWindFlowRenderFrame): void;
    clear(): void;
    destroy(): void;
    private createContextAndResources;
    private drawFade;
    private drawClip;
    private drawSegments;
    private createProgram;
    private createShader;
    private createBuffer;
    private releaseResources;
    private getGl;
    private getProgram;
    private getFadeVertexSource;
    private getFadeFragmentSource;
    private getSegmentVertexSource;
    private getSegmentFragmentSource;
    private getClipVertexSource;
    private getClipFragmentSource;
}
export {};
