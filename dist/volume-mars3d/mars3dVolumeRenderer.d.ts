import type { IVolumeRouteProfile } from '../volume-scene/profileTypes';
import type { IVolumeSource } from '../volume-scene/sourceTypes';
import type { IVolumeLockedSlice, VolumeSliceAxis } from '../volume-scene/sliceTypes';
import type { IMars3dVolumeLayerOptions, IMars3dVolumeLayerUpdate, IMars3dVolumeMapLike } from './volumeMars3dTypes';
/** 在 Mars3D Canvas 上方管理透明 WebGPU 画布、Cesium 相机同步和体积场景生命周期。 */
export declare class Mars3dVolumeRenderer {
    readonly readyPromise: Promise<void>;
    private readonly map;
    private readonly canvas;
    private readonly onError;
    private readonly cameraFrameCompensation;
    private scene;
    private source;
    private layout;
    private frameRequest;
    private cameraEndTimer;
    private framesRemaining;
    private shown;
    private destroyed;
    private cameraMoving;
    private cameraSignature;
    private pendingCompensationFrame;
    private displayedCompensationFrame;
    constructor(map: IMars3dVolumeMapLike, options: IMars3dVolumeLayerOptions);
    get statistics(): import("../volume-scene/sceneTypes").IVolumeSceneStatistics | null;
    get lockedSlices(): readonly IVolumeLockedSlice[];
    setShow(show: boolean): void;
    update(options: IMars3dVolumeLayerUpdate): void;
    setTimeIndex(timeIndex: number): void;
    setRouteProfiles(profiles: readonly IVolumeRouteProfile[]): void;
    updateSlice(id: string, position: number): void;
    removeSlice(id: string): void;
    clearLockedSlices(): void;
    lockSlice(axis: VolumeSliceAxis, position: number): IVolumeLockedSlice;
    setSource(source: IVolumeSource): void;
    destroy(): void;
    private finishCameraInteraction;
    private readonly handlePreRender;
    private readonly handleResize;
    private initialize;
    private syncCamera;
    private bindFrameAvailableCallback;
    private createProjectionCamera;
    private createCurrentCompensationFrame;
    private applyCameraFrameCompensation;
    private clearCameraFrameCompensation;
    private requestFrames;
    private requestFramesImmediately;
    private readonly renderFrame;
    private createCanvas;
    private createCameraSignature;
    private scheduleCameraMoveEnd;
    private beginCameraInteraction;
    private requireScene;
    private assertAlive;
}
