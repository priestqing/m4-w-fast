/**
 * Insert one shared source block into exactly one shader placeholder.
 * 将单个共享源码块插入唯一的 shader 占位符。
 */
export declare const assembleShaderSource: (shaderSource: string, placeholder: string, sharedSource: string) => string;
