import type { IVolumeSource } from '../volume-scene/sourceTypes';
import type { IVolumeLockedSlice, VolumeSliceAxis } from '../volume-scene/sliceTypes';
import type { INativeSliceCesiumRuntime } from './nativeSliceRuntimeTypes';
import type { IMars3dNativeMapLike, IMars3dNativeVolumeLayerOptions, IMars3dNativeVolumeLayerUpdate, INativeVolumeStatistics } from './nativeTypes';
export declare class Mars3dNativeSliceRenderer {
    readonly readyPromise: Promise<void>;
    private readonly map;
    private readonly cesium;
    private readonly onError;
    private readonly axisRenderer;
    private readonly geometryRebuildScheduler;
    private readonly sliceState;
    private readonly unsubscribeSlices;
    private primitive;
    private pendingPrimitive;
    private pendingSwapCleanup;
    private material;
    private source;
    private timeIndex;
    private quality;
    private opacity;
    private densityScale;
    private verticalScale;
    private colorStops;
    private opacityStops;
    private colorMode;
    private shown;
    private destroyed;
    private loadController;
    private atlasCanvas;
    private atlasLayers;
    private nativeData;
    constructor(map: IMars3dNativeMapLike, cesium: INativeSliceCesiumRuntime, options: IMars3dNativeVolumeLayerOptions);
    get statistics(): INativeVolumeStatistics | null;
    get lockedSlices(): readonly IVolumeLockedSlice[];
    lockSlice(axis: VolumeSliceAxis, position: number): IVolumeLockedSlice;
    updateSlice(id: string, position: number): void;
    removeSlice(id: string): void;
    clearLockedSlices(): void;
    setShow(show: boolean): void;
    update(options: IMars3dNativeVolumeLayerUpdate): void;
    setSource(source: IVolumeSource): Promise<void>;
    setTimeIndex(timeIndex: number): Promise<void>;
    destroy(): void;
    private rebuildAtlasAndPrimitive;
    private replacePrimitive;
    private createRectangle;
    private getLayerTextureBounds;
    private getLayerHeight;
    private createMaterialColor;
    private updateMaterialOpacity;
    private scheduleGeometryRebuild;
    private syncAxisSlices;
    private cancelPendingSwap;
    private assertAlive;
}
