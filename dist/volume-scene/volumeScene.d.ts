import type { IVolumeCameraState } from './cameraTypes';
import type { IVolumeRouteProfile } from './profileTypes';
import type { IVolumeFrameCapture, IVolumeScene, IVolumeSceneOptions, IVolumeSceneStatistics, IVolumeSceneUpdate, VolumeInteractionKind } from './sceneTypes';
import type { IVolumeSource } from './sourceTypes';
import type { IVolumeLockedSlice, VolumeSliceAxis, VolumeSliceTool } from './sliceTypes';
export declare class VolumeScene implements IVolumeScene {
    readonly backend: "webgpu";
    private readonly renderer;
    private readonly volumeCamera;
    private readonly canvas;
    private readonly renderMode;
    private readonly interactionElement;
    private readonly eventDisposers;
    private source;
    private layout;
    private descriptors;
    private sliceState;
    private colorStops;
    private colorInputs;
    private opacityStops;
    private profiles;
    private timeIndex;
    private sourceGeneration;
    private loadedKeys;
    private pendingKeys;
    private desiredKeys;
    private frameRequest;
    private destroyed;
    private baseModelMatrix;
    private verticalScale;
    private readonly coordinateSystem;
    private clipBox;
    private quality;
    private autoQuality;
    private autoFrameCount;
    private autoFrameIntervalTotal;
    private colorMode;
    private pointer;
    private navigationMode;
    private pixelRatioCap;
    private renderScale;
    private interactionRenderScale;
    private interactionStepScale;
    private readonly activeInteractions;
    private renderDirty;
    private brickScheduleDirty;
    private brickScheduleCount;
    private brickSortCount;
    private pendingSliceSnapshot;
    private readonly sliceDepthMode;
    private readonly slicePlateColor;
    private readonly onSlicesChange;
    private lastSubmittedFrameTimestamp;
    private lastTimingFrameTimestamp;
    private actualFrameIntervalMilliseconds;
    private readonly requestControllers;
    private sliceInteraction;
    private markerOverlay;
    private constructor();
    static create(options: IVolumeSceneOptions): Promise<VolumeScene>;
    get camera(): IVolumeCameraState;
    get lockedSlices(): readonly IVolumeLockedSlice[];
    get statistics(): IVolumeSceneStatistics;
    update(options: IVolumeSceneUpdate): void;
    setCamera(camera: IVolumeCameraState): void;
    setTimeIndex(timeIndex: number): void;
    setSource(source: IVolumeSource): void;
    setRouteProfiles(profiles: readonly IVolumeRouteProfile[]): void;
    setSliceTool(axis: VolumeSliceTool): void;
    beginInteraction(kind: VolumeInteractionKind): void;
    endInteraction(kind: VolumeInteractionKind): void;
    lockSlice(axis: VolumeSliceAxis, position: number): IVolumeLockedSlice;
    updateSlice(id: string, position: number): void;
    removeSlice(id: string): void;
    clearLockedSlices(): void;
    resize(): void;
    render(): void;
    captureFrame(): Promise<IVolumeFrameCapture>;
    destroy(): void;
    private createLayout;
    private getEffectiveQuality;
    private getStepLength;
    private isInteracting;
    private getInteractionKind;
    private getEffectiveRenderScale;
    private recordFrameInterval;
    private updateAutoQuality;
    private createSliceState;
    private bindSliceState;
    private flushSliceState;
    private bindSliceInteraction;
    private bindControls;
    private configureSource;
    private configureModel;
    private rebuildProfiles;
    private resetBrickRequests;
    private requestBrickSchedule;
    private flushBrickSchedule;
    private scheduleBricks;
    private loadBrick;
    private handleFrameAvailable;
    private requestRender;
}
